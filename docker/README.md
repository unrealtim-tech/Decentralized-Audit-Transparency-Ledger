# Local development stack

Run the full stack (placeholder images and services):

```bash
cd docker
docker compose up --build
```

This composes a Stellar quickstart, Redis, an API WebSocket server, a UI, and a contract build container. Contract deployment and initialization steps are left as placeholders to be adapted to your environment.
